# larql-compute cross-platform CI
#
# Validates the CPU baseline on Linux, Windows, and macOS, plus the macOS-only
# Metal feature gate. Linux/Windows intentionally run `--all-features` too:
# enabling the `metal` feature there must not expose macOS-only modules.

name: larql-compute

on:
  push:
    branches: [main]
    paths:
      - 'crates/larql-compute/**'
      - 'crates/larql-models/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'Makefile'
      - '.github/workflows/larql-compute.yml'
  pull_request:
    branches: [main]
    paths:
      - 'crates/larql-compute/**'
      - 'crates/larql-models/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'Makefile'
      - '.github/workflows/larql-compute.yml'
  workflow_dispatch: {}

jobs:
  test:
    name: test · ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 35

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-14]

    steps:
      - uses: actions/checkout@v4

      - name: Install stable Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Install OpenBLAS (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libopenblas-dev pkg-config

      - name: Install OpenBLAS (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $root = $env:VCPKG_INSTALLATION_ROOT
          if (-not $root) {
            $root = 'C:\vcpkg'
          }
          & "$root\vcpkg.exe" install openblas:x64-windows
          "VCPKG_ROOT=$root" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "$root\installed\x64-windows\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Cache cargo registry + build artefacts
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-compute-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-compute-

      - name: Format check
        run: cargo fmt -p larql-compute -- --check

      - name: Check default features
        run: cargo check -p larql-compute --all-targets

      - name: Check all features on Linux/Windows
        if: runner.os != 'macOS'
        run: cargo check -p larql-compute --all-targets --all-features

      - name: Check Metal feature on macOS
        if: runner.os == 'macOS'
        run: cargo check -p larql-compute --all-targets --features metal

      - name: Clippy default features
        run: cargo clippy -p larql-compute --all-targets -- -D warnings

      - name: Clippy all features on Linux/Windows
        if: runner.os != 'macOS'
        run: cargo clippy -p larql-compute --all-targets --all-features -- -D warnings

      - name: Clippy Metal feature on macOS
        if: runner.os == 'macOS'
        run: cargo clippy -p larql-compute --all-targets --features metal -- -D warnings

      - name: Unit tests
        run: cargo test -p larql-compute --lib

      - name: Backend contract tests
        run: cargo test -p larql-compute --test test_backend_matmul_quant

      - name: Pipeline and MoE contract tests
        run: cargo test -p larql-compute --test test_pipeline_and_moe
